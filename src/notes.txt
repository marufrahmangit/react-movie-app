// Importing useState hook from React to manage local state in functional components
import { useState, useEffect } from 'react'

// These are just image imports (optional, used for logos or branding)
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'

// Importing external styles for the app layout
import './App.css'

// A functional component named "Card" that accepts a prop called "title"
const Card = ({ title }) => {
    const [count, setCount]  = useState(0);
    // Local state for each card to track if it has been liked
    const [hasLiked, setHasLiked] = useState(false);

    useEffect(() => {
        console.log(`${title} has been liked: ${hasLiked}`);
    }, [hasLiked]);

    return (
        // This is the outer container for each card
        <div className="card" onClick={() => setCount(count + 1)}>
            {/* The title of the card is displayed in an <h2> tag */}
            <h2>{ title } - { count }</h2>

            {/* The button allows the user to like the card */}
            <button onClick={() => setHasLiked(!hasLiked)}>
                {hasLiked ? "❤️" : "🤍"}
            </button>
        </div>
    )
}

// The main App component that renders multiple Card components
const AppOldLearning = () => {
    // No state needed here right now since liking is handled inside each Card

    return (
        // This is the container for all the Card components
        <div className="card-container">
            {/* Each Card receives a title prop.
                Other props like rating and isCool are passed to Star Wars but not used yet */}
            <Card title="Star Wars" rating={5} isCool={true} />
            <Card title="Jumanji" />
            <Card title="Prometheus" />
        </div>
    )
}

// Exporting the App component so it can be used in main.jsx and rendered in the browser
export default AppOldLearning


// app.jsx template
------------------

import React from 'react';

function App() {
  return (
    <div>
      <h1>Hello, React!</h1>
      <p>This is a basic React app template.</p>
    </div>
  );
}

export default App;

-------------------